{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the PinShot main and overlay windows.",
  "windows": ["main", "overlay-*"],
  "permissions": ["core:default"]
}
