// Settings window (US3) — placeholder. The full tabbed Settings UI (General,
// Capture, Hotkeys, Annotation, Export, Advanced) backed by the core settings
// schema and the app-shell IPC is implemented in a later pass.

const root = document.getElementById('app');
if (root) {
  root.textContent = 'PinShot Settings — coming soon.';
}
